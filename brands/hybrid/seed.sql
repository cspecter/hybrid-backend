-- Hybrid brand-specific seed data.
-- Keep schema changes out of this file.
-- Add only data/config rows that are unique to the Hybrid backend.

-- Example:
-- insert into public.app_config (key, value)
-- values ('brand_id', '"hybrid"'::jsonb)
-- on conflict (key) do update set value = excluded.value;
