-- Prayer Today brand-specific seed data.
-- Keep schema changes out of this file.
-- Add only data/config rows that are unique to the Prayer Today backend.

-- Example:
-- insert into public.app_config (key, value)
-- values ('brand_id', '"prayertoday"'::jsonb)
-- on conflict (key) do update set value = excluded.value;
